/**
 * These are the settings to connect to the MarkLogic backend
 */
var connection = {
	host: 'localhost',
	port: 8071,
	user: 'admin',
	password: 'admin'
};

module.exports.connection = connection;
